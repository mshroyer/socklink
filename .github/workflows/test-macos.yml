name: Test macOS
on:
  workflow_call:
  push:
    branches:
      - "**"
    tags-ignore:
      - "**"
  pull_request:
  schedule:
    - cron: "57 15 * * 1"

jobs:
  test:
    uses: ./.github/workflows/test.yml
    with:
      os: macos-latest
