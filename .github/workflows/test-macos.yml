name: Test macOS
on:
  workflow_call:
  push:
    branches:
      - "**"
    tags-ignore:
      - "**"
  pull_request:

jobs:
  test:
    uses: ./.github/workflows/test.yml
    with:
      os: macos-latest
