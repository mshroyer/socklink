name: Test Ubuntu
on:
  workflow_call:
  workflow_dispatch:
  push:
    branches:
      - "**"
    tags-ignore:
      - "**"
  pull_request:
  schedule:
    - cron: "57 15 * * 1"

jobs:
  test:
    uses: ./.github/workflows/test.yml
    with:
      os: ubuntu-latest
