[project]
name = "tsock-tests"
version = "0.1"
description = "Integration tests for tsock.sh"
requires-python = ">=3.11"
dependencies = [
    "pexpect>=4.9.0",
    "pytest>=8.4.1",
]

[dependency-groups]
dev = [
    "ruff>=0.12.9",
]
